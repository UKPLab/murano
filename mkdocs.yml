site_name: murano
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: murano
      handlers:
        python:
          options:
            docstring_style: numpy